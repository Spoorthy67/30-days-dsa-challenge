class Solution {
    int count=0;
    public int uniquePaths(int m, int n) {
        helper(m-1,n-1);
        return count;
    }
    public void helper(int m,int n){
        if(m==0||n==0){
            count+=1;
            return;
        }
        helper(m-1,n);
        helper(m,n-1);
    }
}
